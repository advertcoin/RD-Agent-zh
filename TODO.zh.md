我们鼓励在代码中设置 TODOs。但有些 TODOs 更全局。
所以我们把它放在这里。


- [ ] 对齐 components 和 scenarios 中文件的命名。
  - 我们希望在 components（所有场景的可重用组件）和 scenarios（特定场景的组件）中具有相同的命名约定逻辑。
  - 但现在我们有以下不匹配
    - `components` 中的 `coder` 和 `components` 中的 `developer`
- [ ] 文件夹的名称与其中的内容不匹配。
  - 为什么场景在实验中？